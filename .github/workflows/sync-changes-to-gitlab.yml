name: Sync changes to GitLab

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    if: (github.repository == 'adamlui/phind-omnibox')
    runs-on: ubuntu-latest
    env:
      TZ: PST8PDT

    steps:

      - name: Checkout adamlui/phind-omnibox
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Push changes to gitlab.com/adamlui/phind-omnibox
        run: |
          git push --force -o ci.skip \
            https://oauth2:${{ secrets.GITLAB_SYNC_PAT }}@gitlab.com/adamlui/phind-omnibox.git main
